{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "name": "order"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "restaurant": {
      "model": "restaurant"
    },
    "drinks": {
      "via": "order",
      "collection": "drink"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "init",
        "prepare",
        "delivering",
        "done"
      ]
    }
  }
}
